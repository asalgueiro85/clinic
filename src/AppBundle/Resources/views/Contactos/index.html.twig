{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Contactos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Observaciones</th>
                <th>Tipo</th>
                <th>Email</th>
                <th>Numero</th>
                <th>Servicio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('contactos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.observaciones }}</td>
                <td>{{ entity.tipo }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.numero }}</td>
                <td>{{ entity.servicio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('contactos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('contactos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('contactos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
